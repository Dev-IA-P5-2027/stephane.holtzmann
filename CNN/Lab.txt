In this lab, we will use the MNIST dataset (This is a dataset of 60,000 28x28 grayscale images of the 10 digits (0 to 9), along with a test set of 10,000 images)
# https://www.tensorflow.org/api_docs/python/tf/keras/datasets/mnist/load_data. You will use tensorflow and matplotlib.

1) Load and explore the MNIST dataset
2) Display information about the dataset (number of samples and shape of each image)
3) Display some sample images
4) Preprocess the data
5) Build an CNN architecture
6) Compile the model (adam, categorical_crossentropy)
7) Train the model
8) Evaluate the model on the test set
9) Save the trained model
10)Plot the training history

