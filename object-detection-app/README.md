# Object Detection App

Mini application Streamlit conteneurisée avec Docker.

## Lancer en local avec Docker

```bash
docker build -t object-detection-app .
docker run -p 8501:8501 object-detection-app